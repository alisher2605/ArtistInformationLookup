<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artistinformationlookup">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".FirebaseApp"
        >
        <activity android:name=".Activities.Information.FavoritesArtistsActivity"></activity>
        <activity android:name=".Activities.Authorization.LoginActivity" />
        <activity android:name=".Activities.Authorization.RegistrationActivity" />
        <activity android:name=".Activities.Information.TrackVideoActivity" />
        <activity android:name=".Activities.Information.AlbumInfoActivity" />
        <activity android:name=".Activities.Information.ArtistAlbumActivity" />
        <activity android:name=".Activities.Information.ArtistInfoActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>